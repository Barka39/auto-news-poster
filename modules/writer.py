"""
Нийтлэл бичих модуль — Groq API (үнэгүй)
Хэрэглэгчийн загварын дагуу: анхаарал татсан эхлэл + 2-3 догол мөр.
Давталт илрүүлэгч + кирилл шалгалттай. Муу гаралт постлогдохгүй.
"""

import os
import re
import logging
import requests
from modules import gemini_compare

log = logging.getLogger(__name__)

GROQ_API_URL = "https://api.groq.com/openai/v1/chat/completions"
# qwen/qwen3.6-27b 2026-09-д Groq-оос хасагдаж 404 өгсөн. Дараагийн удаа
# secret/variable-ээр л солихын тулд орчны хувьсагчаас уншина.
GROQ_MODEL = os.environ.get("GROQ_MODEL", "qwen/qwen3.8-27b")

# Шинэ нарийвчилсан категориуд (basketball/football/ufc) БҮГД sports
# найруулгын prompt ашиглана. Энэ alias байхгүй бол SYSTEM_PROMPTS.get()
# world_news (неутраль, ишлэлгүй) style руу УНАЖ, спортын ярианы өнгө
# алдагдана.
CATEGORY_STYLE = {
    "basketball": "sports",       # NBA
    "mn_basketball": "sports",    # Монголын сагсан бөмбөг
    "football": "sports",
    "ufc": "sports",
}

# Ач холбогдлын онооны босго (0-10). Үүнээс доош мэдээ ПОСТЛОГДОХГҮЙ —
# "юу ч болоогүй" гэдэг нь сул пост гаргаснаас дээр (хуудасны чанар).
MIN_NEWS_SCORE = int(os.environ.get("MIN_NEWS_SCORE", "6"))

SYSTEM_PROMPTS = {
    "sports": """Чи Монголын шилдэг спортын контент бичигч. Фэнүүдийн дунд хэллэгээр,
товч гэхдээ утга дүүрэн бичдэг.

ХАМГИЙН ЧУХАЛ ЗАРЧИМ — НЭГ ЗАГВАРААР БҮҮ БИЧ: Чи темплэйт бөглөдөг
робот биш, мэргэжлийн спортын редактор. Мэдээ бүр өөр бүтэц, өөр эхлэл,
өөр төгсгөлтэй байх ёстой. Дараалсан постууд ижил өгүүлбэрийн загвартай
харагдвал дагагчид "бот бичсэн" гэдгийг шууд мэдэрнэ.

МЭДЭЭНИЙ ТӨРЛӨӨ ЭХЭЛЖ ТАНЬЖ, ТОХИРОХ ХЭВ МАЯГААР БИЧ:
1) ТОГЛОЛТЫН ҮР ДҮН → Тоо, стат голлоно: оноо, шийдвэрлэх мөч, гол
   тоглогчийн stat line. Төгсгөл нь дараагийн тоглолт/хүлээлт байж болно.
2) ШИЛЖИЛТ, ГЭРЭЭ → Мөнгөн дүн, хугацаа, нөхцөл + энэ нь багт юу гэсэн
   үг болох.
3) МАРГААН, ЗОДООН, СКАНДАЛ → ЮУ БОЛСОН, ЮУ ХЭЛСЭН нь постын зүрх.
   Шалтгаан → үр дагаврыг тодорхой гарга.
4) ГЭМТЭЛ → Байдал, эдгэрэх хугацаа, оронд нь хэн тоглох.
5) ЯРИЛЦЛАГА, МЭДЭГДЭЛ → Ишлэл түрүүлж, тайлбар ард нь.
6) ДЭЭД АМЖИЛТ → Ямар амжилт, өмнөх эзэн нь хэн байсан, түүхэн жин.
Ижил төрлийн мэдээн дээр ч бүтцээ сэлгэж бай: заримдаа статаар эхэл,
заримдаа ишлэлээр, заримдаа нэг хурц өгүүлбэрээр.

ХЭЛ ЯРИАНЫ ЖИШЭЭ (энэ маягийн БАЙГАЛИЙН, ярианы хэллэгээр бич):
"Hawks-Lakers гэсэн багууд урьд өмнө нь наймаа хийж байсан болохоор
харьцангуй харилцаа сайтай багууд гэдгийг сануулъя. Өмнө нь Vincent-г
Kennard болгож буян үйлдсэн, энэ удаад Vanderbilt-г Kuminga болгох яриа
явагдаж л байна." — Ийм маягийн БАЙГАЛИЙН, чөлөөтэй, ярианы урсгалтай
өгүүлбэр бич. Хатуу, албан ёсны орчуулгын хэв маягаас зайлсхий.

ИШЛЭЛЭЭР ЭХЛЭХ ЗАГВАР (эх мэдээлэлд хэн нэгний хэлсэн үг байвал ашигла):
Хэрэв тохиромжтой бол ийм байдлаар эхэл:
"[Монгол орчуулсан ишлэл]"
- [Хүний нэр] [сэдвийн тухай] ингэж хэлжээ/бичжээ/ярьжээ.
Дараа нь агуулгаа үргэлжлүүл.

ИШЛЭЛГҮЙ ҮЕД: Хэрэв тохиромжтой ишлэл байхгүй бол, богино анхаарал
татсан өгүүлбэрээр эхэл (жишээ: "Los Angeles Lakers шинэ эрин үеэ
эхлүүлэхээр боллоо."), дараа нь 2-3 богино догол мөрөөр агуулгаа тайлбарла.

ЯРИАНЫ ӨНГӨ (зэрэг тааруулж хэрэглэ, хэтрүүлэхгүй):
- "л" үгийг эмфазд ашиглаж болно (жишээ: "болов уу л даа", "явагдаж л байна")
- "гэдэг" -ийг цуурхал/яриа тэмдэглэхэд ашиглаж болно (жишээ: "...гэдэг
  сурагтай", "...гэдэг яриа гарна")
- Хэт албан бус, найз нөхөдтэйгээ ярьж байгаа мэт БОЛОВЧ МЭРГЭЖЛИЙН
  хэвээр байх — жинхэнэ инээдийн тэмдэг ("хаха" гэх мэт) бүү ашигла,
  тэр хэтэрхий санамсаргүй харагдана

ХУВИЙН ДҮГНЭЛТ (сонголттой — ойролцоогоор 3 постын 1-д л): Ихэнх пост
дүгнэлтгүйгээр — баримт, стат, дараагийн хүлээлтээрээ — төгсөх нь зүйтэй.
Дүгнэлт нэмэх бол 1 өгүүлбэр, зохиогчийн хувийн үзэл гэдэг нь илт байх
ёстой. ГЭХДЭЭ ХЭЛЛЭГЭЭ БАЙНГА СОЛЬЖ БАЙ: "Миний бодлоор ... л болов уу
даа" гэсэн ЯГ ИЖИЛ загварыг пост болгонд давтахыг ХОРИГЛОНО (бодит
алдаа: бүх пост ингэж төгсөөд робот бичсэн мэт харагдсан). Хувилбарууд:
"Хувьдаа бол...", "...гэдэгт эргэлзэж байна", "...нь сонин байх нь ээ",
"Харин ... эсэхийг цаг харуулна", риторик асуулт, эсвэл огт дүгнэлтгүй.
ХЭЗЭЭ Ч таамаглалыг баримт мэт бүү бич.

"ТЭГЭЭД ЮУ ГЭСЭН ЮМ?" ДҮРЭМ (маш чухал): Мэдээ хэн нэгний хэлсэн үг,
шүүмжлэл, маргаан дээр тогтож байвал ЯГ ЮУ хэлснийг (Монгол орчуулгаар)
заавал оруул — энэ л постын хамгийн үнэ цэнтэй хэсэг. Эх материалд
хэлсэн үг нь огт байхгүй бол "яг юу гэж бичсэн нь ил болоогүй байна"
гэдгийг нэг өгүүлбэрээр шууд хэл; "шүүмжилсэн нь анхаарал татаж байна"
маягийн хоосон, тойруу өгүүлбэрээр орлуулахыг ХОРИГЛОНО. Уншигч постыг
уншаад "тэгээд юу гэсэн юм бэ?" гэсэн асуулттай үлдэж болохгүй.

ХЭМЖЭЭНИЙ ХАТУУ ДҮРЭМ (маш чухал): Нийт 2-4 БОГИНО догол мөр,
ойролцоогоор 400-700 тэмдэгт. Уншигч Facebook дээр гүйлгэж байхдаа
15-20 секундэд уншиж дуусахаар байх ёстой. Урт, нуршуу пост уншигчийг
уйдааж, engagement-ийг бууруулна. Хамгийн чухал 2-3 баримт + (хүсвэл)
1 өгүүлбэр дүгнэлт — ингээд Л ХАНГАЛТТАЙ. Бүх дэлгэрэнгүйг багтаах
гэж БҮҮ оролд.

НЭГ ПОСТ = НЭГ СЭДЭВ (хамгийн чухал дүрэм): Хэрэв эх мэдээлэлд ХЭД
ХЭДЭН ХООРОНДОО ХОЛБООГҮЙ мэдээ (жишээ: Summer League-ийн тоглолт +
өөр багийн солилцоо + WNBA-ийн мэдээ) байвал — энэ нь олон сэдэвт
тойм нийтлэл гэсэн үг. ЗӨВХӨН ХАМГИЙН ГОЛ (эсвэл хамгийн эхний) НЭГ
сэдвийг сонгож, ЗӨВХӨН түүний тухай бич. Бусад сэдвүүдийг БҮРЭН ОРХИ —
нэг ч өгүүлбэрээр бүү дурд. Тойм маягийн, олон сэдэв дараалуулсан
пост бичих нь ХОРИОТОЙ.

ДОГОЛ МӨРИЙН ДҮРЭМ: НЭГ сэдвийн доторх өөр өөр талыг (жишээ: НЭГ
тоглогчийн гэмтэл + түүний оронд хэн тоглох + дасгалжуулагчийн
сэтгэгдэл) тусдаа догол мөрт (хоосон мөрөөр тусгаарлаж) бич. Нэг
догол мөрт зөвхөн НЭГ баримт/санаа байх ёстой.

АСУУЛТ ОРЧУУЛАХ ДҮРЭМ: Эх текст дэх risторик асуулт ("Who can stop
France?" гэх мэт) байвал ҮГ ҮГЭЭР бүү орчуул. Байгалийн Монгол асуулт
болго. Жишээ БУРУУ: "Францын аварга болох замыг хаахад хэн чадна вэ?"
Жишээ ЗӨВ: "Францын аварга болох замд хэн саад болж чадах вэ?"

НЭРНИЙ ХАТУУ ДҮРЭМ (маш чухал):
- Тоглогч, багийн НЭР бүхэлдээ Англи (Латин) үсгээр байх ЁСТОЙ — нэг ч үсэг Кирилл рүү бүү хөрвүүл
- ЗӨВШӨӨРӨГДӨХГҮЙ АЛДАА (хэзээ ч БҮҮ хий): нэрийг хагасыг нь Кирилл, хагасыг нь Латин бичих.
  Жишээ БУРУУ: "Энцо Fernandez" (Энцо гэдэг хэсэг Кирилл болсон — АЛДАА)
  Жишээ ЗӨВ: "Enzo Fernandez" (бүхэлдээ Латин)
  Жишээ БУРУУ: "Марин Ле Пен" (бүхэлдээ Кирилл болсон)
  Жишээ ЗӨВ: "Marine Le Pen" (бүхэлдээ Латин)
- Тэмцээний шат/үе шатны нэрийг (Final, Playoffs гэх мэт) Англи хэвээр эсвэл Монгол
  үгтэй хослуулж болно (жишээ: "Бүсийн Final", "Conference Final")

АНГЛИАР ХЭВЭЭР ҮЛДЭЭХ СПОРТЫН НЭР ТОМЬЁО (Монгол тайлбарлагчид ихэвчлэн
англиар нь хэрэглэдэг тул орчуулахгүй, англи хэвээр бич):
Triple-double, Double-double, Buzzer shoot, Clutch, Poster, And-one,
Pick and roll, Pick and pop, Step-back, Euro step, Iso
(мөн энэ маягийн бусад олон улсын нийтлэг спортын нэр томьёог адилхан
англи хэвээр нь ашиглаж болно)

HASHTAG: Нийтлэлийн ЭЦЭСТ сэдэвт тохирсон 1-2 Англи hashtag нэм
(жишээ: #LebronToWarriors, #TradeRumors, #Waived) — SWI$H хуудасны
загварын адил бүтээлч, сэдэвт нийцсэн байх.
HASHTAG-ИЙН ХАТУУ ДҮРЭМ: Тэмцээн/лигийн нэрийг (World Cup, Euro,
Olympics, NBA Finals г.м.) hashtag-д ЗӨВХӨН эх мэдээлэлд ЯГ тэр нэрээр
дурдагдсан бол ашигла. Эх текстэнд байхгүй тэмцээний нэрийг ХЭЗЭЭ Ч
таамаглаж бүү бич (жишээ: World Cup-ын мэдээнд #Euro2024 тавих нь
НОЦТОЙ баримтын алдаа). Эргэлзвэл тэмцээний нэргүй, сэдвийн hashtag
(жишээ: #InjuryNews) ашигла.

БУСАД ДҮРЭМ:
- Нэг өгүүлбэрийг ХЭЗЭЭ Ч давтахгүй, нэг санааг нэг л удаа хэл
- Зөвхөн өгөгдсөн мэдээлэлд байгаа баримтыг ашигла, шинэ БАРИМТ бүү зохио
  (гэхдээ дээрх ХУВИЙН ТААМАГЛАЛ дүрмийн дагуу тодорхой хэлбэлзэлтэй
  дүгнэлт нэмж болно)
- Гарчиг, эх сурвалж, линк бүү бич
- Бусад бүх өгүүлбэрийг МОНГОЛ хэлээр бич (нэр, тэмцээний шат, тусгай нэр
  томьёоноос бусад)""",

    "music": """Чи Монголын хөгжим/шоу бизнесийн контент бичигч. Залуучуудын
хэллэгээр товч, сонирхолтой бичдэг.

БИЧИХ ЗАГВАР (заавал дага):
1-р мөр: Богино, анхаарал татсан өгүүлбэр
Дараа нь: 2-3 богино догол мөр

ХАТУУ ДҮРЭМ:
- Нэг өгүүлбэрийг ХЭЗЭЭ Ч давтахгүй
- Дуучин, жүжигчний нэрийг Англи үсгээр, дуу/киноны нэрийг "" хашилтад
- Зөвхөн өгөгдсөн баримтыг ашигла
- Гарчиг, эх сурвалж, линк, hashtag бүү бич
- Бүх өгүүлбэр МОНГОЛ хэлээр (нэрсээс бусад)""",

    "world_news": """Чи Монголын олон улсын мэдээний контент бичигч. Нейтрал,
ойлгомжтой, дэвсгэр тайлбартай бичдэг.

БИЧИХ ЗАГВАР (заавал дага):
1-р мөр: Богино, анхаарал татсан өгүүлбэр
Дараа нь: 2-3 богино догол мөр

НЭРНИЙ ХАТУУ ДҮРЭМ (маш чухал):
- Хүний бүтэн нэр (нэр + овог) Англи (Латин) үсгээр байх ЁСТОЙ — нэг ч
  үсэг Кирилл рүү бүү хөрвүүл
- ЗӨВШӨӨРӨГДӨХГҮЙ АЛДАА (хэзээ ч БҮҮ хий): нэрийг хагасыг нь Кирилл,
  хагасыг нь Латин бичих.
  Жишээ БУРУУ: "Марин Ле Пен" (бүхэлдээ Кирилл болсон — АЛДАА)
  Жишээ ЗӨВ: "Marine Le Pen" (бүхэлдээ Латин)
- Улс, хотын нэрийг Монгол дуудлагаар бич (жишээ: Лондон, Бээжин, Франц)

ДОГОЛ МӨРИЙН ДҮРЭМ: Хэрэв эх мэдээлэлд ХЭД ХЭДЭН ТУСДАА баримт/дэд сэдэв
байвал, тэдгээрийг ТУСДАА ДОГОЛ МӨРТ (хоосон мөрөөр тусгаарлаж) бич.

БУСАД ДҮРЭМ:
- Нэг өгүүлбэрийг ХЭЗЭЭ Ч давтахгүй
- Зөвхөн өгөгдсөн баримтыг ашигла
- Гарчиг, эх сурвалж, линк, hashtag бүү бич
- Бусад бүх өгүүлбэрийг МОНГОЛ хэлээр бич (хүний нэрнээс бусад)"""
}

_PROPER_NOUN_RE = re.compile(r"\b[A-Z][a-zA-Z'.-]*\b")


def _clean_output(text: str) -> str:
    text = text.strip()
    if text.startswith("```"):
        parts = text.split("```")
        text = parts[1] if len(parts) > 1 else text
        text = text.strip()
    lines = text.split("\n")
    if lines and (lines[0].strip().endswith(":") or
                  re.match(r"^(here|орчуулга|нийтлэл|article|translation)", lines[0].strip(), re.IGNORECASE)):
        lines = lines[1:]
    return "\n".join(lines).strip()


def _has_repetition(text: str) -> bool:
    """
    Llama-гийн давталтын алдааг илрүүлэх:
    - Ижил өгүүлбэр 2+ удаа (яг таарсан)
    - Ижил 6 үгийн хэлц 2+ удаа
    - Утгын давхардал: өөр үгээр ижил санааг 2+ удаа хэлсэн
      (жишээ: "Х өөрчлөгдөж байна" / "Х-ийн өөрчлөлтийг онцолж байна")
    """
    sentences = [s.strip().lower() for s in re.split(r"[.!?]\s+", text) if len(s.strip()) > 20]
    if len(sentences) != len(set(sentences)):
        return True

    words = text.lower().split()
    seen = set()
    for i in range(len(words) - 5):
        gram = " ".join(words[i:i + 6])
        if gram in seen:
            return True
        seen.add(gram)

    # Утгын давхардал — өгүүлбэр хоорондын үгийн давхцлыг шалгах (Jaccard)
    STOP = {"нь", "нэг", "энэ", "тэр", "бол", "гэж", "гэдэг", "болно", "байна",
            "байгаа", "хэрэг", "явдал", "хийж", "болж", "их", "бас"}
    sentence_word_sets = []
    for s in sentences:
        sig_words = {w for w in re.findall(r"[а-яөүёА-ЯӨҮЁ]{4,}", s) if w not in STOP}
        if len(sig_words) >= 2:
            sentence_word_sets.append(sig_words)

    for i in range(len(sentence_word_sets)):
        for j in range(i + 1, len(sentence_word_sets)):
            a, b = sentence_word_sets[i], sentence_word_sets[j]
            overlap = len(a & b) / len(a | b)
            if overlap >= 0.35:
                return True

    return False


def is_valid_mongolian(text: str, min_len: int = 120) -> bool:
    """Кирилл харьцаа + давталтын шалгалт"""
    if not text or len(text) < min_len:
        return False

    text_no_names = _PROPER_NOUN_RE.sub("", text)
    letters = [c for c in text_no_names if c.isalpha()]
    if not letters:
        return False
    cyr = [c for c in letters if "\u0400" <= c <= "\u04FF"]
    if len(cyr) / len(letters) < 0.85:
        return False

    if _has_repetition(text):
        log.warning("Давталт илэрлээ — гаралтыг хаялаа")
        return False

    return True


def _try_qwen(system_prompt: str, user_prompt: str) -> str:
    """Qwen (Groq)-оор бичих оролдлого хийх, амжилттай бол текстийг буцаана"""
    api_key = os.environ.get("GROQ_API_KEY")
    if not api_key:
        return ""

    for attempt in range(2):
        try:
            response = requests.post(
                GROQ_API_URL,
                headers={
                    "Authorization": f"Bearer {api_key}",
                    "Content-Type": "application/json"
                },
                json={
                    "model": GROQ_MODEL,
                    "messages": [
                        {"role": "system", "content": system_prompt},
                        {"role": "user", "content": user_prompt}
                    ],
                    "temperature": 0.5,
                    "max_tokens": 800,
                    "frequency_penalty": 0.8,
                    "reasoning_effort": "none"
                },
                timeout=30
            )
            response.raise_for_status()
            data = response.json()
            message = data["choices"][0]["message"]
            article_text = _clean_output(message.get("content", ""))

            if is_valid_mongolian(article_text):
                log.info(f"Qwen нийтлэл OK [{GROQ_MODEL}] (оролдлого {attempt+1}): {article_text[:60]}...")
                return article_text

            log.warning(f"Qwen гаралт чанаргүй (оролдлого {attempt+1}) — дахин оролдоно")

        except Exception as e:
            log.error(f"Qwen (Groq) API алдаа (оролдлого {attempt+1}): {e}")

    return ""


def write_article(news: dict) -> dict:
    """
    Нийтлэл бичих дараалал (үнэ цэнэ буурах эрэмбээр):
    1. Gemini — бодит харьцуулалтад Qwen-с илүү нарийвчлалтай гарсан тул ҮНДСЭН
    2. Qwen (Groq) — Gemini амжилтгүй бол нөөц
    3. Google Translate — хоёул амжилтгүй бол эцсийн нөөц
    """
    category = news.get("category", "world_news")
    style_key = CATEGORY_STYLE.get(category, category)
    system_prompt = SYSTEM_PROMPTS.get(style_key, SYSTEM_PROMPTS["world_news"])
    system_prompt += """

НАЙРУУЛГЫН ЧУХАЛ ДҮРЭМ:
- Англи эх текстийг ҮГ ҮГЭЭР бүү орчуул. Утгыг нь бүрэн ойлгоод, эхнээс
  бүтэн, зөв дүрмийн (нөхцөл, тийн ялгал зөв) МОНГОЛ өгүүлбэрээр дахин
  найруул. Өгүүлбэр бүр өөрөө дангаараа ойлгомжтой байх ёстой.
- Хэрэв эх мэдээлэлд ХОЛБООГҮЙ хэд хэдэн сэдэв (жишээ: өөр өөр хүний
  тухай тусдаа мэдээ) зэрэгцүүлж орсон бол ЗААВАЛ хамгийн гол, хамгийн
  шинэ НЭГ сэдвийг сонгож ЗӨВХӨН түүн дээр төвлөр. Бусад хажуугийн
  сэдвийг ОГТ БҮҮ ДУРД — нэг пост нэг л мэдээ байна. (Урьд нь "тусдаа
  өгүүлбэрт бич" гэсэн сонголт олон сэдэвт замбараагүй пост үүсгэсэн.)
- ҮГИЙН СОНГОЛТЫН ХАТУУ ДҮРЭМ (өмнө гарсан бодит алдаанууд):
  * Тэмцээнээс хасагдахыг "хасагдсан" гэж бич — ХЭЗЭЭ Ч "шүүгдсэн" гэж
    бүү бич (энэ нь шүүхэд шүүгдэх гэсэн утгатай, буруу).
  * Багийн ахлагчийг "ахлагч" гэж бич. "агуулаг" гэдэг үг байхгүй.
    Монгол үг + Англи ижил утгатай үгийг зэрэгцүүлж давхарлахгүй
    (жишээ: "ахлагч Captain" гэж бүү бич — аль нэгийг нь л сонго).
  * Шагнал хүртсэн хүнийг "шагналын эзэн" гэж бич ("шагналт" биш).
  * Эргэлзсэн үгээ Монголоор яаж хэлэхээ мэдэхгүй бол ойролцоо ховор
    үг таамаглахын оронд энгийн, түгээмэл үгээр тайлбарлаж бич.
- Ижил үг/хэллэгийг ойрхон давтахгүй байх (жишээ: "өнөөдрийн өнөөдөр"
  гэх мэт санамсаргүй давхардал гаргахгүй).
- НЭГ ӨГҮҮЛБЭРТ ЗӨВХӨН НЭГ ТОДОРХОЙ БАРИМТ/ҮЙЛДЭЛ бич. Хэд хэдэн үйлдэл,
  шалтгаан, нөхцлийг нэг урт, төвөгтэй өгүүлбэрт бүү шахаж хол. Хэрэв эх
  текст дэх санаа нарийн төвөгтэй бол 2-3 БОГИНО, ТОДОРХОЙ өгүүлбэрт
  задалж бич — урт бөгөөд ойлгомжгүй нэг өгүүлбэрээс богино, тодорхой
  хэд хэдэн өгүүлбэр илүү дээр.
- Өгүүлбэр бичихийн өмнө өөрөөсөө асуу: "Энэ өгүүлбэрийг Монгол хүн нэг
  удаа уншаад шууд ойлгох уу?" Хэрэв эргэлзвэл богиносгож, энгийн бол.
- БАРИМТЫГ АЛДАЖ БОЛОХГҮЙ: цаг агаарын үзэгдэл (үер/тайфун/аянга гэх
  мэт), тоо, нэр, газар зэргийг эх текстээс яг зөв, өөрчлөлтгүй авч
  орчуул. Тодорхойгүй бол ойролцоо биш, хамгийн үнэн зөв нэр томьёог
  ашигла.
- СТАТИСТИК, ТОО БАРИМТЫГ ЗААВАЛ ОРУУЛ: хэрэв эх мэдээлэлд тоглолтын
  оноо, гол/оноо тоо, хувь хэмжээ, мөнгөн дүн, он сар өдөр, статистик
  үзүүлэлт зэрэг АЛЬ Ч тоо баримт байвал — эдгээрийг ЗААВАЛ, тодорхой
  оруул. Уншигчид хамгийн их сонирхдог зүйл бол яг ийм нарийн тоо
  баримт (жишээ: "3-1 тоглолт хожив", "25 оноо авав", "хагас цагт 2
  гол оруулав") юм. Хэрэв эх мэдээлэлд тоо баримт байхгүй бол зохиож
  бүү нэм — зөвхөн байгаа бол чухалчлан харуул гэсэн үг.
- ТОДОРХОЙГҮЙ/ТЕХНИК СЭДВИЙГ ТАЙЛБАРЛАЖ ОЙЛГОМЖТОЙ БОЛГО: хэрэв эх
  мэдээлэл шинжлэх ухаан, судалгаа, техник зэрэг ойлгоход хэцүү
  сэдэвтэй бол, ЗӨВХӨН эх текстийг шууд орчуулаад орхихгүй — "энэ нь
  юу гэсэн үг вэ", "энэ яагаад чухал вэ" гэдгийг НЭГ нэмэлт өгүүлбэрээр
  энгийн, амьдралын жишээгээр тайлбарла. Жишээ нь "судалгаагаар X
  илэрсэн" гэхээс илүү, "X илэрсэн нь Y гэсэн үг" гэж тодруул. Эх
  мэдээлэлд байхгүй тайлбарыг зохиож болохгүй — зөвхөн эх дэх
  мэдээллийг илүү ойлгомжтой байдлаар дахин найруулж бич."""

    extra_parts = []
    if news.get("og_description"):
        extra_parts.append(f"Товч тайлбар (эх сайтын og:description): {news['og_description']}")
    if news.get("body_excerpt"):
        extra_parts.append(f"Өгүүллийн эхний хэсгийн бодит текст: {news['body_excerpt']}")
    # ЯАГААД: RSS-ийн summary ганцаараа ихэвчлэн 1 өгүүлбэр (эсвэл хоосон)
    # байдаг тул шинжилгээт мэдээнд (жишээ: "6 баг өрсөлдөж байна") Gemini
    # ямар ч нарийн зүйл (баг нэрс, тоо баримт) бичих материалгүй болдог
    # байсан. Эх хуудаснаас татсан нэмэлт текст үүнийг шийднэ.
    extra_block = ("\n" + "\n".join(extra_parts) + "\n") if extra_parts else ""

    kind_note = ""
    if news.get("kind") == "game_recap":
        kind_note = """
ЭНЭ БОЛ ДӨНГӨЖ ДУУССАН ТОГЛОЛТЫН ҮР ДҮН (ESPN scoreboard өгөгдөл).
ЗААВАЛ: эхний өгүүлбэрт хэн хэнийг ямар оноогоор хожсоныг; дараа нь
шилдэг тоглогчдын stat line-ийг ТООГООР НЬ (оноо/самбар/дамжуулалт);
багуудын улирлын амжилтыг (W-L). Хэрэв OT байвал заавал дурд. Өгөгдөлд
байхгүй тоглолтын явцыг (хэн хэзээ ямар шидэлт хийсэн) ЗОХИОЖ БҮҮ БИЧ. Багийн бүсийн байр (тэргүүлж байна, playoff-ийн бүсэд орлоо г.м.), дараалсан ялалт/хожигдлын тоог ЗӨВХӨН өгөгдөлд шууд бичигдсэн бол л дурд — W-L амжилтаас өөрөө дүгнэж бүү бич.
"""
    if news.get("lang") == "mn":
        kind_note += """
ЭХ МЭДЭЭЛЭЛ МОНГОЛ ХЭЛЭЭР БАЙНА (Монголын сагсан бөмбөгийн мэдээ).
Орчуулах биш, ӨӨРИЙН ҮГЭЭР, ӨӨР БҮТЭЦТЭЙ дахин найруулж бич — эх текстийн
өгүүлбэрийг үг үгээр хуулахыг ХОРИГЛОНО (зохиогчийн эрх). Баг, тоглогч,
тэмцээний Монгол нэрийг Монголоор нь хэвээр үлдээ (Латин болгохгүй).
"""

    user_prompt = f"""МЭДЭЭЛЭЛ:
Гарчиг: {news['title']}
Агуулга: {news.get('summary', '')}
{extra_block}{kind_note}
Дээрх мэдээллээр Монгол нийтлэл бич. Зөвхөн нийтлэлийн текстийг бич,
өөр юу ч бүү нэм."""

    # 1. ҮНДСЭН: Gemini
    if gemini_compare.is_enabled():
        for attempt in range(2):
            article_text = gemini_compare.generate(system_prompt, user_prompt)
            article_text = _clean_output(article_text) if article_text else ""

            if is_valid_mongolian(article_text):
                news["article_mn"] = article_text
                log.info(f"✅ Gemini нийтлэл OK (оролдлого {attempt+1}): {article_text[:60]}...")
                return news

            log.warning(f"Gemini гаралт чанаргүй/хоосон (оролдлого {attempt+1}) — дахин оролдоно")

        log.warning("Gemini 2 оролдлого бүтэлгүйтлээ — Qwen (Groq) руу шилжлээ")

    # 2. НӨӨЦ: Qwen (Groq)
    log.info(f"writer.py нөөц загвар: {GROQ_MODEL}")
    article_text = _try_qwen(system_prompt, user_prompt)
    if article_text:
        news["article_mn"] = article_text
        return news

    # 3. ЭЦСИЙН НӨӨЦ: Google Translate
    log.warning("Gemini болон Qwen хоёул бүтэлгүйтлээ — Google Translate руу шилжлээ")
    return _fallback(news)


def _fallback(news: dict) -> dict:
    from modules.translator import translate_to_mongolian
    translated = translate_to_mongolian(news)

    title_mn = translated.get("title_mn", "")
    summary_mn = translated.get("summary_mn", "")
    article = f"{title_mn}\n\n{summary_mn}".strip()

    if is_valid_mongolian(article, min_len=40):
        news["article_mn"] = article
    else:
        log.error("Орчуулга бүрэн бүтэлгүйтлээ — энэ мэдээг алгасна")
        news["article_mn"] = ""

    return news


def _score_prompt(candidates: list, now_ts: float) -> str:
    def _age_label(n):
        ts = n.get("published_ts", 0)
        if not ts:
            return "цаг тодорхойгүй"
        return f"{(now_ts - ts) / 3600:.1f} цагийн өмнө"

    listing = "\n".join(
        f"{i+1}. [{n.get('category_mn', '')}] ({_age_label(n)}) {n['title']}"
        + (f" — {n.get('summary', '')[:160]}" if n.get("summary") else "")
        for i, n in enumerate(candidates)
    )
    return f"""Чи NBA болон МОНГОЛЫН САГСАН БӨМБӨГИЙН фэнүүдэд зориулсан Монгол
Facebook хуудасны ахлах редактор. Доорх мэдээ бүрд 0-10 оноо өг.

ОНООНЫ ШАЛГУУР:
10 = Монголын сагсны ТОМ мэдээ (үндэсний шигшээ, 3x3 шигшээ, MNBA/The League
     финал, Монгол тоглогч гадаадад) ЭСВЭЛ NBA-ийн дөнгөж болсон ТОМ үйл явдал
     (playoff/финалын үр дүн, супер одны трейд/гэрээ, ноцтой гэмтэл, рекорд).
8-9 = NBA-ийн шинэ, тодорхой, баримттай нэг мэдээ (тоглолтын үр дүн, гэрээ,
     трейд, гэмтэл, шийтгэл, одны мэдэгдэл), эсвэл Монголын сагсны ердийн мэдээ.
6-7 = Бодит боловч жижиг мэдээ (роль тоглогчийн гэрээ, two-way, waive, удирдлагын
     томилгоо, зодог тайлалт, шүүхийн/лигийн шалгалт) — постлогдоно.
5 = Сонирхолтой боловч баримт багатай дүн шинжилгээ; зөвхөн маш сонирхолтой бол.
0-4 = Preview/урьдчилсан тойм, power rankings, podcast, live blog, олон сэдэвт
     тойм, fantasy зөвлөгөө, худалдааны зар, хуучин дүн шинжилгээ, NCAA/WNBA/
     G-League/Summer League (үнэхээр том биш бол), сагсан бөмбөгтэй огт
     ХОЛБООГҮЙ бүх зүйл (хөл бөмбөг, MMA г.м. = 0).

Шинэ цагтай (цөөн цагийн өмнөх) мэдээ хуучнаас дээгүүр. Гарчиг
"хэн юу хийсэн" гэсэн НЭГ тодорхой үйл явдлыг хэлж байвал дээгүүр.

{listing}

ЗӨВХӨН мөр бүрд "дугаар: оноо" гэж бич (жишээ: 1: 9). Өөр текст бүү нэм."""


def _parse_scores(raw: str, n: int) -> dict:
    scores = {}
    for m in re.finditer(r"(\d+)\s*[:=\-]\s*(\d+(?:\.\d+)?)", raw or ""):
        idx, val = int(m.group(1)) - 1, float(m.group(2))
        if 0 <= idx < n and 0 <= val <= 10:
            scores[idx] = val
    return scores


def score_news(candidates: list) -> dict:
    """Кандидат бүрд 0-10 оноо. Groq → бүтэлгүй бол Gemini. Хоёулаа
    бүтэлгүй бол {} (дуудагч нь юу ч постлохгүй)."""
    import time as _time
    prompt = _score_prompt(candidates, _time.time())
    system = "You are a strict sports news editor. Score each item 0-10 per the rubric. Output only 'number: score' lines."

    api_key = os.environ.get("GROQ_API_KEY")
    if api_key:
        try:
            response = requests.post(
                GROQ_API_URL,
                headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
                json={"model": GROQ_MODEL,
                      "messages": [{"role": "system", "content": system},
                                   {"role": "user", "content": prompt}],
                      "temperature": 0.1, "max_tokens": 300, "reasoning_effort": "none"},
                timeout=25)
            response.raise_for_status()
            scores = _parse_scores(response.json()["choices"][0]["message"]["content"], len(candidates))
            if scores:
                return scores
            log.warning("Онооны шүүлтүүр (Groq) хоосон буцлаа — Gemini-д шилжье")
        except Exception as e:
            log.warning(f"Онооны шүүлтүүр (Groq) алдаа: {e} — Gemini-д шилжье")

    if gemini_compare.is_enabled():
        try:
            scores = _parse_scores(gemini_compare.generate(system, prompt), len(candidates))
            if scores:
                return scores
        except Exception as e:
            log.warning(f"Онооны шүүлтүүр (Gemini) алдаа: {e}")
    return {}


def filter_relevant_news(news_list: list, max_candidates: int = 25) -> list:
    """
    Мэдээ бүрд ач холбогдлын оноо өгч, MIN_NEWS_SCORE-оос ДООШ бүгдийг
    хаяна; үлдсэнийг оноогоор эрэмбэлнэ (n["score"] талбар нэмэгдэнэ).
    Хуучин хувилбар "сонгосон дугаар" буцаадаг байсан бөгөөд шүүлтүүр
    унахад бүх мэдээг өнгөрүүлдэг байсан (2026-09: Groq 404 → 2 долоо хоног
    шүүлтүүргүй постолсон). Одоо: оноо авч чадахгүй бол ЮУ Ч постлохгүй —
    дараагийн run 5-15 минутын дараа дахин оролдоно.
    """
    if not news_list:
        return news_list
    candidates = news_list[:max_candidates]
    scores = score_news(candidates)
    # S4: Монголын шигшээ/тоглогч/лиг дурдагдсан мэдээ = 10 (LLM-ээс үл хамааран)
    from modules.fetcher import is_mn_watch
    for i, n in enumerate(candidates):
        if is_mn_watch(n.get("title", ""), n.get("summary", ""), n.get("lang", "")):
            scores[i] = 10
            log.info(f"[МОНГОЛ WATCH] 10 оноо: {n['title'][:60]}")
    if not scores:
        log.warning("Онооны шүүлтүүр ажилласангүй — энэ run-д юу ч постлохгүй")
        return []
    kept = []
    for i, n in enumerate(candidates):
        sc = scores.get(i, 0)
        n["score"] = sc
        if sc >= MIN_NEWS_SCORE:
            kept.append(n)
        else:
            log.info(f"[ОНОО {sc:.0f}] хасав: {n['title'][:60]}")
    kept.sort(key=lambda n: (n.get("score", 0), n.get("published_ts", 0)), reverse=True)
    for n in kept:
        log.info(f"[ОНОО {n['score']:.0f}] үлдээв: {n['title'][:60]}")
    log.info(f"Ач холбогдлын шүүлтүүр: {len(candidates)} -> {len(kept)} мэдээ (босго {MIN_NEWS_SCORE})")
    return kept


def polish_article(news: dict, draft: str, lint_note: str = "") -> str:
    """
    РЕДАКТОРЫН ХОЁР ДАХЬ ДАМЖЛАГА. Нэг дуудлагаар бичсэн нийтлэлд байнга
    гардаг гурван алдааг засна: (1) эх материалд байхгүй баримт нэмэгдсэн,
    (2) орчуулгын калька/хатуу хэллэг, (3) тоо баримт алдагдсан эсвэл
    hook-гүй эхлэл. Эх баримт + ноорог хоёуланг өгч, редактор ЗӨВХӨН
    эцсийн текст буцаана. Чанарын шалгалт давахгүй бол ноорог хэвээр.
    """
    if not draft or not gemini_compare.is_enabled():
        return draft
    facts = [f"Гарчиг: {news.get('title', '')}", f"Агуулга: {news.get('summary', '')}"]
    if news.get("og_description"):
        facts.append(f"Тайлбар: {news['og_description']}")
    if news.get("body_excerpt"):
        facts.append(f"Өгүүллийн текст: {news['body_excerpt'][:2500]}")
    system = """Чи Монголын спортын хэвлэлийн АХЛАХ РЕДАКТОР. Залуу сэтгүүлчийн
бичсэн Facebook постыг эх баримттай тулгаж, хэвлэлд гарахад бэлэн болго.
Чи ШИНЭ пост бичихгүй — ноорогийн сайн талыг хадгалж, доорх шалгуураар
ЗАСНА. Зөвхөн эцсийн текстийг буцаа: гарчиг, тайлбар, "Засвар:" гэх мэт
юу ч бүү нэм."""
    user = f"""ЭХ БАРИМТ (үнэний цорын ганц эх сурвалж):
{chr(10).join(facts)}

НООРОГ:
{draft}

{("ЗААВАЛ ЗАС (автомат шалгалт олсон): " + lint_note + chr(10) + chr(10)) if lint_note else ""}ШАЛГУУР (дарааллаар нь шалга):
1. БАРИМТ: ноорогт эх баримтад БАЙХГҮЙ тоо, нэр, шалтгаан, ишлэл, дүгнэлт
   орсон бол ХАС эсвэл эх баримтаар соль. Эх баримтад байгаа чухал тоо
   (оноо, stat line, мөнгөн дүн, хугацаа, W-L) ноорогт орхигдсон бол НЭМ.
2. НЭР: тоглогч, баг, лигийн нэр бүхэлдээ Латин үсгээр (Монголын баг,
   тоглогч бол Монголоор). Хагас Кирилл/хагас Латин нэрийг засна.
3. ЭХЛЭЛ: эхний өгүүлбэр 15 үгээс богино, мэдээний ГОЛ баримтыг хэлдэг
   байг (юу болсон / хэн хожсон / ямар гэрээ). "Сонирхолтой мэдээ",
   "Мэдээ ирлээ" маягийн хоосон эхлэлийг хас.
4. ХЭЛ: Англи бүтцээр орчуулсан хатуу өгүүлбэрийг Монгол хүн ярьдаг
   байгалийн хэллэг болго. Нэг өгүүлбэрт нэг санаа. Давтагдсан үг/санааг хас.
   "шүүгдсэн", "агуулаг", "шагналт" гэх мэт буруу үг хэрэглэсэн бол засна.
5. НЭР ТОМЬЁОНЫ ТОЛЬ (нэг стандарт): rebound = самбар; assist = дамжуулалт;
   steal = таслалт; block = хаалт; turnover = алдаа; free throw = чөлөөт шидэлт;
   three-pointer = 3 оноо; field goal = талбайн шидэлт; overtime = нэмэлт цаг (OT);
   playoff/play-in/double-double/triple-double/clutch = Латинаар; coach = дасгалжуулагч;
   ТРЕЙД/ГЭРЭЭНИЙ ЗААВАЛ БАЙХ ЗҮЙЛ: хэн хэнийг, АЛЬ БАГААС АЛЬ БАГТ, юуны хариуд
   (тоглогч, драфтын эрх), ямар дүн/хугацаагаар — эх материалд байгаа бүх нэр, тоог
   заавал дурд. Зөвхөн "N солигдлоо" гэж бичихийг ХОРИГЛОНО.
   roster = бүрэлдэхүүн; trade = трейд/солилцоо; extension = гэрээний сунгалт;
   waive = гэрээ цуцлах; two-way = two-way гэрээ; rookie = шинэ тоглогч (rookie).
6. ХЭМЖЭЭ: 2-4 догол мөр, 400-700 тэмдэгт. Нэг сэдэв. Эцэст нь 1-2 hashtag
   (байхгүй бол нэм, гурваас олон бол цөөл; тэмцээний нэрийг эх баримтад
   байгаа үед л hashtag болго).
7. Бүх зүйл зөв бол ноорогийг ЯГ ХЭВЭЭР буцаа.

ЭЦСИЙН ТЕКСТ:"""
    try:
        polished = _clean_output(gemini_compare.generate(system, user) or "")
    except Exception as e:
        log.warning(f"Редакторын дамжлага алдаа: {e} — ноорог хэвээр")
        return draft
    if not polished or not is_valid_mongolian(polished, min_len=120):
        log.warning("Редакторын гаралт чанаргүй — ноорог хэвээр")
        return draft
    if not (200 <= len(polished) <= 1100):
        log.warning(f"Редакторын гаралт хэмжээ хэтэрсэн ({len(polished)}ch) — ноорог хэвээр")
        return draft
    if polished.lower().startswith(("эцсийн текст", "засвар", "ноорог")):
        return draft
    log.info(f"✍️ Редакторын дамжлага: {len(draft)}ch → {len(polished)}ch")
    return polished


def write_digest(news_list: list) -> str:
    """
    Хэд хэдэн мэдээг НЭГ тойм постонд нэгтгэж бичнэ. Мэдээ бүр тусдаа
    догол мөрт, байгалийн Монгол хэллэгээр. Gemini-г ашиглана (Qwen
    нөөц). Амжилтгүй бол хоосон текст буцаана.
    """
    if not news_list:
        return ""

    items_text = "\n\n".join(
        f"[{n.get('category_mn', '')}] {n['title']}: {n.get('summary', '')[:300]}"
        for n in news_list
    )

    system_prompt = """Чи Монголын мэдээний тойм бичигч. Хэд хэдэн мэдээг
НЭГ тойм постонд нэгтгэж бичнэ.

ЗААВАЛ дагах дүрэм:
- Мэдээ БҮРИЙГ тусдаа догол мөрт (хоосон мөрөөр тусгаарлаж) бич
- Мэдээ бүрийг 1-2 өгүүлбэрт багтаан товч, тодорхой бич
- Тоглогч/хүний/багийн нэрийг Англи (Латин) үсгээр бич, хагас Кирилл
  хагас Латин болгож нэрийг хуваахгүй
- Улс, хотын нэрийг Монгол дуудлагаар
- Байгалийн, чөлөөтэй ярианы хэллэгээр бич — хатуу албан орчуулгаас
  зайлсхий
- Гарчиг, хураангуй тайлбар, hashtag бүү нэм — зөвхөн мэдээ бүрийн
  агуулгыг дараалуулан бич
- Зөвхөн өгөгдсөн баримтыг ашигла, шинэ баримт бүү зохио"""

    user_prompt = f"""Доорх мэдээнүүдийг тус бүрийг тусдаа догол мөрт
нэгтгэн Монгол хэлээр бич:

{items_text}"""

    if gemini_compare.is_enabled():
        text = gemini_compare.generate(system_prompt, user_prompt)
        text = _clean_output(text) if text else ""
        if is_valid_mongolian(text, min_len=60):
            log.info(f"Тойм пост Gemini-ээр бичигдлээ ({len(news_list)} мэдээ)")
            return text

    # Нөөц: Qwen (Groq)
    text = _try_qwen(system_prompt, user_prompt)
    if text:
        return text

    return ""
